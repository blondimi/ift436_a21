\documentclass{article}

%% Packages
\usepackage[utf8]{inputenc}
\usepackage[french]{babel}
\usepackage{amsmath,amssymb}
\usepackage{enumerate}
\usepackage[noline, onelanguage, french, noend]{algorithm2e}
\usepackage{tikz}
\usetikzlibrary{positioning, automata}

%% Macros
\newcommand{\N}{\mathbb{N}}
\renewcommand{\O}{\mathcal{O}}
\newcommand{\algdelim}{\texttt{:}}
\newcommand{\algskip}{\vspace*{3pt}}
\newcommand{\emptyseq}{[\,]}
\newcommand{\lto}{\,{:}\,}
\newcommand{\G}{\mathcal{G}}
\newcommand{\vrai}{\texttt{vrai}}
\newcommand{\faux}{\texttt{faux}}
\newcommand{\trans}[1]{\xrightarrow{#1}}

%% Titre
\title{IFT436: devoir 3}
\author{Foo McBar}
\date{13 octobre 2021}

\begin{document}
\maketitle

%% Contenu
\section*{Question 1}

\begin{enumerate}[(a)]
\item $u \trans{} v$, $\O(n)$, ...

\item ...

\item ...

      \begin{center}
        \begin{tikzpicture}[auto, node distance=2cm, thick,
                            transform shape, scale=0.9]
        \tikzstyle{astate} = [state, minimum size=15pt];

        %% Sommets
        \node[astate]             (0) {};
        \node[astate, right of=0] (1) {};
        \node[astate, below of=0, xshift=0.5cm, yshift=0.5cm] (2) {};
        \node[astate, below of=1] (3) {};
        \node[astate, right of=1] (4) {};
        \node[astate, below right=-0.5cm and 3cm of 3] (5) {};
        \node[astate, below right= 0.5cm and 1cm of 4] (6) {};

        %% Arêtes marquées
        \path[-, orange, line width=5pt]
        (3) edge node {} (4)
        ;

        %% Arêtes
        \path[-]
        (0) edge node {$1$} (1)
        (0) edge node[swap] {$1$} (2)
        (2) edge node[swap] {$3$} (3)
        (1) edge node {$2$} (4)
        (3) edge node {$5$} (4)
        (4) edge node[swap] {$2$} (5)
        (3) edge node[swap] {$3$} (5)
        (5) edge node[swap] {$1$} (6)
        (4) edge node {$2$} (6)
        (1) edge node {$2$} (2)
        ;

      \end{tikzpicture}
    \end{center}


\item ...

    \begin{minipage}[t]{\textwidth}
      \begin{algorithm}[H]
        \DontPrintSemicolon
        %\LinesNumbered
        \Entree{un graphe non dirigé $\G = (V, E)$}
        \Sortie{la plus petite quantité de téléphones à installer
                pour obtenir une subvention}
        \algskip
        $T \leftarrow [v \mapsto \faux : v \in V]$; $c \leftarrow 0$\;
        \algskip
        
        \While{$V$ est non vide}{
          \textbf{retirer} un sommet $x$ de $V$\;
          \algskip

          \lIf{$\neg T[x]$ ou $(\neg T[y]$ pour au moins
               une certaine arête $(x, y) \in E)$}{
            $T[x] \leftarrow \vrai$; $c \leftarrow c + 1$
          }
        }

        \algskip
        \Return{$c$}
        
      \end{algorithm}
    \end{minipage}
  
\end{enumerate}

\end{document}
